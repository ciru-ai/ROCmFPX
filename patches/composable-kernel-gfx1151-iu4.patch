diff --git a/include/ck/tensor_operation/gpu/block/blockwise_gemm_pipeline_wmmaops_base.hpp b/include/ck/tensor_operation/gpu/block/blockwise_gemm_pipeline_wmmaops_base.hpp
index fb1ae8c54..d3244e3c3 100644
--- a/include/ck/tensor_operation/gpu/block/blockwise_gemm_pipeline_wmmaops_base.hpp
+++ b/include/ck/tensor_operation/gpu/block/blockwise_gemm_pipeline_wmmaops_base.hpp
@@ -54,8 +54,11 @@ struct BlockwiseGemmWmmaops_pipeline_base
     static constexpr index_t A_KRow = 2;
     static constexpr index_t B_KRow = 2;
 #else
-    static constexpr index_t A_KRow = 1;
-    static constexpr index_t B_KRow = 1;
+    // pk_i4_t stores two logical WMMA operands in one byte.  The gfx11
+    // IU4 instruction still consumes K=16, but its register fragment is
+    // eight packed bytes rather than sixteen scalar elements.
+    static constexpr index_t A_KRow = is_same<ComputeTypeA, pk_i4_t>::value ? 2 : 1;
+    static constexpr index_t B_KRow = is_same<ComputeTypeB, pk_i4_t>::value ? 2 : 1;
 #endif
 
     static constexpr auto wmma_gemm = WmmaGemm<ComputeTypeA,
diff --git a/include/ck/tensor_operation/gpu/warp/wmma_gemm.hpp b/include/ck/tensor_operation/gpu/warp/wmma_gemm.hpp
index 09cf0ec87..478ba5add 100644
--- a/include/ck/tensor_operation/gpu/warp/wmma_gemm.hpp
+++ b/include/ck/tensor_operation/gpu/warp/wmma_gemm.hpp
@@ -293,6 +293,44 @@ struct wmma_type<WmmaInstr::wmma_i32_16x16x16_iu8,
     }
 };
 
+template <>
+struct wmma_type<WmmaInstr::wmma_i32_16x16x16_iu4, 32, void>
+{
+    static constexpr index_t m_per_wmma               = 16;
+    static constexpr index_t n_per_wmma               = 16;
+    // CK counts storage elements here. Each uint8_t carries two logical
+    // signed INT4 values, so the K=16 hardware instruction advances 8 bytes.
+    static constexpr index_t k_per_wmma               = 8;
+    static constexpr index_t k_per_blk                = 8;
+    static constexpr index_t src_a_data_size          = 1;
+    static constexpr index_t src_b_data_size          = 1;
+    static constexpr index_t acc_data_size            = 4;
+    static constexpr index_t acc_pack_number          = 1;
+    static constexpr index_t num_thread_per_subgroups = n_per_wmma;
+    static constexpr index_t wave_size                = 32;
+    static constexpr index_t num_src_a_vgprs_per_wave =
+        m_per_wmma * src_a_data_size / 4;
+    static constexpr index_t num_src_b_vgprs_per_wave =
+        n_per_wmma * src_b_data_size / 4;
+    static constexpr index_t num_acc_vgprs_per_wave =
+        m_per_wmma * n_per_wmma * acc_data_size * acc_pack_number / wave_size / 4;
+    static constexpr index_t num_subgroups = wave_size / num_thread_per_subgroups;
+
+    template <index_t MPerWmma,
+              index_t NPerWmma,
+              class FloatA,
+              class FloatB,
+              class FloatC,
+              bool neg_a = true,
+              bool neg_b = true,
+              bool clamp = false>
+    __device__ void run(const FloatA& a, const FloatB& b, FloatC& reg_c) const
+    {
+        intrin_wmma_i32_16x16x16_iu4_w32<MPerWmma, NPerWmma, neg_a, neg_b, clamp>::Run(
+            a, b, reg_c);
+    }
+};
+
 // gfx12
 
 // A-swizzled
@@ -608,6 +646,24 @@ struct WmmaSelector
 #endif
     }
 
+    template <>
+    constexpr auto GetWmma<pk_i4_t, pk_i4_t, int, 16, 16>()
+    {
+        return WmmaInstr::wmma_i32_16x16x16_iu4;
+    }
+
+    template <>
+    constexpr auto GetWmma<uint8_t, uint8_t, int, 16, 16>()
+    {
+        return WmmaInstr::wmma_i32_16x16x16_iu4;
+    }
+
+    template <>
+    constexpr auto GetWmma<uint8_t, int8_t, int, 16, 16>()
+    {
+        return WmmaInstr::wmma_i32_16x16x16_iu4;
+    }
+
 #ifdef CK_EXPERIMENTAL_BIT_INT_EXTENSION_INT4
     template <>
     constexpr auto GetWmma<int4_t, int4_t, int, 16, 16>()
@@ -650,7 +706,8 @@ struct WmmaSelector
 
         static_assert(selected_wmma.m_per_wmma == 16, "WRONG! WMMA_M must equal to 16");
 
-        static_assert(selected_wmma.k_per_wmma == 16, "WRONG! WMMA_M must equal to 16");
+        static_assert(selected_wmma.k_per_wmma == 16 || selected_wmma.k_per_wmma == 8,
+                      "WRONG! unsupported WMMA K storage width");
 
         static_assert(selected_wmma.wave_size * selected_wmma.num_acc_vgprs_per_wave *
                               selected_wmma.acc_data_size * selected_wmma.acc_pack_number ==
@@ -792,6 +849,12 @@ struct WmmaGemm
                  is_same<dst_type, bhalf_t>::value) ||
                 (is_same<src_type_a, int8_t>::value && is_same<src_type_b, int8_t>::value &&
                  is_same<dst_type, int32_t>::value) ||
+                (is_same<src_type_a, uint8_t>::value && is_same<src_type_b, uint8_t>::value &&
+                 is_same<dst_type, int32_t>::value) ||
+                (is_same<src_type_a, uint8_t>::value && is_same<src_type_b, int8_t>::value &&
+                 is_same<dst_type, int32_t>::value) ||
+                (is_same<src_type_a, pk_i4_t>::value && is_same<src_type_b, pk_i4_t>::value &&
+                 is_same<dst_type, int32_t>::value) ||
                 ((is_same<src_type_a, f8_t>::value || is_same<src_type_a, bf8_t>::value) &&
                  (is_same<src_type_b, f8_t>::value || is_same<src_type_b, bf8_t>::value) &&
                  is_same<dst_type, float>::value) ||
@@ -808,7 +871,22 @@ struct WmmaGemm
             // hardcoded.
             if constexpr(!TransposeC)
             {
-                wmma_instr.template run<MPerWmma, NPerWmma>(p_a_wave[k], p_b_wave[k], p_c_thread);
+                if constexpr(is_same<src_type_a, uint8_t>::value &&
+                             is_same<src_type_b, int8_t>::value)
+                {
+                    wmma_instr.template run<MPerWmma,
+                                            NPerWmma,
+                                            decltype(p_a_wave[k]),
+                                            decltype(p_b_wave[k]),
+                                            FloatC,
+                                            false,
+                                            true>(p_a_wave[k], p_b_wave[k], p_c_thread);
+                }
+                else
+                {
+                    wmma_instr.template run<MPerWmma, NPerWmma>(
+                        p_a_wave[k], p_b_wave[k], p_c_thread);
+                }
             }
             else
             {
diff --git a/include/ck/utility/amd_wmma.hpp b/include/ck/utility/amd_wmma.hpp
index 057687985..6db86fa40 100644
--- a/include/ck/utility/amd_wmma.hpp
+++ b/include/ck/utility/amd_wmma.hpp
@@ -144,6 +144,35 @@ struct intrin_wmma_i32_16x16x16_iu8_w32<16, 16, neg_a, neg_b, clamp>
     }
 };
 
+// src: iu4, dst: i32
+template <index_t MPerWave, index_t NPerWave, bool neg_a, bool neg_b, bool clamp>
+struct intrin_wmma_i32_16x16x16_iu4_w32;
+
+template <bool neg_a, bool neg_b, bool clamp>
+struct intrin_wmma_i32_16x16x16_iu4_w32<16, 16, neg_a, neg_b, clamp>
+{
+    template <class FloatA, class FloatB, class FloatC>
+    __device__ static void Run(const FloatA& reg_a, const FloatB& reg_b, FloatC& reg_c)
+    {
+        static_assert(sizeof(FloatA) == sizeof(int32x2_t), "IU4 A fragment must occupy two VGPRs");
+        static_assert(sizeof(FloatB) == sizeof(int32x2_t), "IU4 B fragment must occupy two VGPRs");
+#if defined(__gfx11__)
+        reg_c.template AsType<int32x8_t>()(Number<0>{}) =
+            __builtin_amdgcn_wmma_i32_16x16x16_iu4_w32(
+                neg_a,
+                bit_cast<int32x2_t>(reg_a),
+                neg_b,
+                bit_cast<int32x2_t>(reg_b),
+                reg_c.template AsType<int32x8_t>()[Number<0>{}],
+                clamp);
+#else
+        ignore = reg_a;
+        ignore = reg_b;
+        ignore = reg_c;
+#endif
+    }
+};
+
 /********************************WAVE64 MODE***********************************************/
 
 template <index_t MPerWave, index_t NPerWave>
diff --git a/include/ck/utility/dtype_vector.hpp b/include/ck/utility/dtype_vector.hpp
index b6a199bd4..236d1e074 100644
--- a/include/ck/utility/dtype_vector.hpp
+++ b/include/ck/utility/dtype_vector.hpp
@@ -651,6 +651,7 @@ using int8x16_t = typename vector_type<int8_t, 16>::type;
 using int8x32_t = typename vector_type<int8_t, 32>::type;
 using int8x64_t = typename vector_type<int8_t, 64>::type;
 
+
 // f8
 using f8x2_fnuz_t  = typename vector_type<f8_fnuz_t, 2>::type;
 using f8x4_fnuz_t  = typename vector_type<f8_fnuz_t, 4>::type;
